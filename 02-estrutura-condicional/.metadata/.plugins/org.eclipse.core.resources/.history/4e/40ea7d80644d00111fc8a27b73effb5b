
public class Exemplo {

	public static void main(String[] args) {
		
		//Sem Ternário
		
		double preco = 35.5;
		double desconto;
		if (preco < 20.0) {
			desconto = preco * 0.1;
		}
		else {
			desconto = preco * 0.05;
		}
		
		System.out.println("Resultado conta sem ternário: " + desconto);
		
		//Com Ternária
		
		double precoT = 35.5;
		double descontoT = (preco < 20.0) ? preco * 0.1 : preco * 0.05;
		
		System.out.println("Resultado conta com ternário"descontoT);

	}

}
